import gettext

_ = gettext.gettext


class banyourwords:
    forever = _('因為 <code>{reason}</code> 而被琦玉用 <code>普通連續拳</code> 永久揍飛於宇宙之外。')
    temp = _(
        '因為 <code>{reason}</code> 而被琦玉用 <code>普通連續拳</code> 暫時揍飛於宇宙之外\n封鎖到：<code>{date}</code>')
